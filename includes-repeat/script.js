let house = ['Gryffindor', 'Ravenclaw', 'Slytherin', 'Hufflepuff'];

// If contains the value returns true otherwise false
console.log( house.includes('Ravenclaw') );

let houseCup = 'Gryffindor';

// The same but with string
console.log( houseCup.includes('ff') );

// Repeat the string X times
let xTimes = 'x';
console.log( xTimes.repeat(20) );